{
    "description": "Result containing the background daemon's version",
    "type": "object",
    "properties": {
        "version": {
            "type": "string",
            "description": "The CLI version of the running daemon"
        }
    }
}